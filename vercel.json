{
  "buildCommand": "npm run build:prod && cp -r ./.build/dist .",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
