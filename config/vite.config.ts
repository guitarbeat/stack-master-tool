 // Re-export root vite config for backwards compatibility with package.json scripts
 // TODO: Update package.json scripts to remove --config flag once possible
 export { default } from '../vite.config';